# Job Board Environment Configuration
# Copy this file to .env and update the values

# JWT Secret - MUST be changed in production!
# Generate a secure random string using: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# Admin Credentials
ADMIN_USERNAME=admin

# SECURITY: Store password as bcrypt hash (recommended) or plain text (legacy)
#
# RECOMMENDED: Use a bcrypt hash for better security
# Generate hash using: node backend/scripts/hash-password.js <your-password>
# Example bcrypt hash: $2b$10$abc123...xyz789
#
# The system will auto-detect if the password is hashed or plain text
# Plain text passwords will show a security warning in the logs
ADMIN_PASSWORD=change-this-password-in-production

# Company Branding (Optional)
# These will be displayed in the header and PWA metadata
COMPANY_NAME=Job Board
COMPANY_SHORT_NAME=Job Board

# Application Settings
NODE_ENV=production

# Application Port (default: 3000)
# This controls both the port mapping and the internal server port
APP_PORT=3000

# Note: If running the backend directly (not via Docker), use PORT instead:
# PORT=3000
